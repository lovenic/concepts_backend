# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Example of extracting secrets from 1password (or another compatible pw manager)
SECRETS=$(kamal secrets fetch --adapter 1password --account $OP_USER_ID --from shinsei/backend-production KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY PRIMARY_DB_HOST CACHE_DB_HOST QUEUE_DB_HOST CABLE_DB_HOST FASTMAIL_SMTP_PASSWORD RC_WEBHOOK_AUTH APPLE_BUNDLE_IDENTIFIER ROLLBAR_ACCESS_TOKEN)
KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
PRIMARY_DB_HOST=$(kamal secrets extract PRIMARY_DB_HOST ${SECRETS})
CACHE_DB_HOST=$(kamal secrets extract CACHE_DB_HOST ${SECRETS})
QUEUE_DB_HOST=$(kamal secrets extract QUEUE_DB_HOST ${SECRETS})
CABLE_DB_HOST=$(kamal secrets extract CABLE_DB_HOST ${SECRETS})
FASTMAIL_SMTP_PASSWORD=$(kamal secrets extract FASTMAIL_SMTP_PASSWORD ${SECRETS})
RC_WEBHOOK_AUTH=$(kamal secrets extract RC_WEBHOOK_AUTH ${SECRETS})
APPLE_BUNDLE_IDENTIFIER=$(kamal secrets extract APPLE_BUNDLE_IDENTIFIER ${SECRETS})
ROLLBAR_ACCESS_TOKEN=$(kamal secrets extract ROLLBAR_ACCESS_TOKEN ${SECRETS})
# Use a GITHUB_TOKEN if private repositories are needed for the image
# GITHUB_TOKEN=$(gh config get -h github.com oauth_token)
